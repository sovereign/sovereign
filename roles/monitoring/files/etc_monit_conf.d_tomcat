check process tomcat with pidfile "/var/run/{{ tomcat_package_name }}.pid"
  group mail
  start program = "systemctl start {{ tomcat_package_name }}"
  stop program = "systemctl stop {{ tomcat_package_name }}"
  if failed port 8080 then alert
  if failed port 8080 for 5 cycles then restart
